#pragma once

// Defines if we have to launch the unit tests when starting the program, won't start the game, only call the unit tests
#define UNIT_TESTS

// Defines the current language
#define ENGLISH
